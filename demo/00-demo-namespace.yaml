apiVersion: v1
kind: Namespace
metadata:
  name: vgpu-demo
  labels:
    # User workloads should run 'restricted' (only the control-plane ns needs privileged).
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted

