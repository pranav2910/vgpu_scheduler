apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: vgpujobs.infrastructure.pranav2910.com
spec:
  group: infrastructure.pranav2910.com
  names:
    kind: VGPUJob
    listKind: VGPUJobList
    plural: vgpujobs
    singular: vgpujob
    shortNames:
      - vjob
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required:
                - claimTemplate
              properties:
                priority:
                  type: integer
                  minimum: 0
                  maximum: 1000
                  default: 50
                workloadClass:
                  type: string
                  enum:
                    - Training
                    - Inference
                    - Batch
                    - Interactive
                  default: Batch
                preemptible:
                  type: boolean
                  default: false
                preemptionGraceSeconds:
                  type: integer
                  minimum: 1
                  maximum: 3600
                  default: 30
                claimTemplate:
                  type: object
                  required:
                    - spec
                  properties:
                    spec:
                      type: object
                      required:
                        - requestedVramBytes
                      properties:
                        requestedVramBytes:
                          type: integer
                          format: int64
                          minimum: 1
                          # Mirror of the vgpuclaims CRD max (sweep S6): without
                          # it a 10TiB job is ACCEPTED here, then its claim is
                          # rejected by the claims CRD -> the job wedges in
                          # silent infinite backoff with empty status.
                          maximum: 85899345920
                        serviceTier:
                          type: string
                          enum:
                            - Guaranteed
                            - BestEffort
                          default: Guaranteed
                        jobRef:
                          type: string
                # podTemplate, when set, makes the VGPUJob own its workload Pod:
                # the controller creates a Pod from this template once the slice
                # is allocated. It is a native corev1.PodTemplateSpec — we don't
                # hand-write its (huge) schema, we preserve it as-is.
                podTemplate:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              properties:
                phase:
                  type: string
                claimRef:
                  type: string
                podRef:
                  type: string
                nodeName:
                  type: string
                deviceUuid:
                  type: string
                message:
                  type: string
                conditions:
                  type: array
                  items:
                    type: object
                    properties:
                      type: { type: string }
                      status: { type: string }
                      reason: { type: string }
                      message: { type: string }
                      lastTransitionTime: { type: string, format: date-time }
                      observedGeneration: { type: integer }
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Priority
          type: integer
          jsonPath: .spec.priority
        - name: Class
          type: string
          jsonPath: .spec.workloadClass
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Claim
          type: string
          jsonPath: .status.claimRef
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
